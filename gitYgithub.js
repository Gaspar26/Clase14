/* Git y Github

                            GIT

--> git: sistema de conrol de versiones que permite a los desarrolladores realizar un seguimiento a las versiones antiguas del codigo, trabajar en paralelo en diferentes ramas y fusionar los cambios en una unica linea principal

RECORDAR IGNORAR NODE_MODULES:

    1-Crear archivo gitignore
    2-Linea de comando node_modules

--> comandos de git
    1- git init
    2- git add . (agrega todo) - git add index.html(por ejemplo agrega una sola seccion o archivo)
    3- git status (muestra informacion de los recursos enviados)
    4- git commit -m "mensaje" 
    5- git push -u origin master || origin main

    --> buenas practicas con commits
        - usar verbos que describa la accion: agregar, cambiar, remover
        - deberan ser cortos y concisos
        - agregaran un prefijo (chore: para realizar tareas - fix: para arreglar bugs)
        ejemplo: git commit -m "chore: remover el search de la navbar"


                        GITHUB

    --> proporciona una interfaz web facil de usar que se integra con git, lo que hace mas facil para los desarrolladores colaborar en proyecto y compartir codigos con otros